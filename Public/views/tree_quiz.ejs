<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tree Quiz</title>
    <link rel="stylesheet" href="/style/nav.css">
<style>
     * {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  }

body {
  font-family: "Poppins", sans-serif;
  margin: 0;
  padding: 0;
  background: linear-gradient(135deg, #74ABE2, #5563DE);
  color: #fff;
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 100vh;
}

/* Adjust content below navbar */
.content-wrapper {
  margin-top: 90px; /* navbar height */
  width: 100%;
  display: flex;
  justify-content: center;
  padding: 0 15px; /* prevents overflow on small screens */
  box-sizing: border-box;
}

.quiz_container {
  background: #ffffff15;
  backdrop-filter: blur(15px);
  border-radius: 16px;
  width: 100%;
  max-width: 800px; /* keeps container inside screen */
  padding: 30px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.3);
  transition: all 0.3s ease;
  margin-bottom: 60px;
}

h1 {
  text-align: center;
  margin-bottom: 25px;
  color: #fff;
  font-size: 2rem;
}

.question {
  background: rgba(255,255,255,0.1);
  padding: 15px;
  border-radius: 10px;
  margin-bottom: 20px;
  transition: all 0.3s ease;
}

.question:hover {
  background: rgba(255,255,255,0.2);
}

.question h3 {
  font-size: 1.1rem;
  margin-bottom: 10px;
  color: #f3f3f3;
}

label {
  display: block;
  margin-bottom: 8px;
  cursor: pointer;
}

input[type="radio"] {
  accent-color: #00ffbf;
  margin-right: 8px;
}

button {
  background: #00ffc8;
  border: none;
  color: #333;
  padding: 12px 25px;
  font-size: 1rem;
  border-radius: 10px;
  cursor: pointer;
  margin: 20px auto 0;
  display: block;
  transition: all 0.3s ease;
}

button:hover {
  background: #00bfa5;
  color: #fff;
  transform: scale(1.05);
}

/* ===== Responsive ===== */
@media screen and (max-width: 900px) {
  .quiz_container {
    padding: 20px;
  }

  h1 {
    font-size: 1.6rem;
  }

  .question h3 {
    font-size: 1rem;
  }
}

@media screen and (max-width: 600px) {
  /* Ensure navbar links stay inside screen */
  .content-wrapper {
    padding: 0 10px;
  }

  .quiz_container {
    padding: 15px;
  }

  h1 {
    font-size: 1.4rem;
  }

  .question h3 {
    font-size: 0.95rem;
  }

  label {
    font-size: 0.9rem;
  }
}
</style>


</head>
<body>

    <%- include('partials/nav') %>
    
     <div class ="content-wrapper">
       <div class="quiz_container">
         <h1>Tree Quiz</h1>

          <form  action="/submit-tree-quiz" method="POST" id="treeForm">

             <% const questions = [

                   {
                      q: "1 . Maximum number of nodes at level l in a binary tree ? ",
                      opts: ["2^l" , "2^(l-1)" , "l²" , "l"]
                   },
                   {
                      q: "2 . In a BST, where will a new element be inserted ? ",
                      opts: ["At root" , "At leaf position" , "Always left side" , "Random position"]
                   } ,
                   {
                      q: "3 . Time complexity of search in a balanced BST ? ",
                      opts: ["O(1)" , "O(n logn)" , "O(n)" , "O(logn)"]

                   },
                   {
                     q: "4 . In a complete binary tree ? ",
                     opts: [ "All levels are completely filled" , 
                     "All levels are filled except possibly last, filled from left" ,
                      "Tree is balanced" , "Every node has 2 children"]
                   },
                   {
                      q: "5 . A full binary tree is a tree in which ? ",
                      opts: ["Every node has 2 children" , 
                      "Every node has 0 or 2 children" ,"All leaves are at same level" 
                      , "Each node has at most 1 child"]

                   },
                   {
                      q: "6 . Which traversal gives sorted output in a Binary Search Tree (BST) ? ",
                      opts: ["PreOrder" , "PostOrder" , "InOrder" , "LevelOrder"]

                   },
                   {
                      q: "7 . The minimum number of nodes in a binary tree of height h ? ",
                      opts: ["h" , "2^h - 1" , 
                      "h + 1" , "1"]

                   },
                   {
                      q: "8 . Which traversal is used to delete a binary tree ? ",
                      opts: ["PreOrder" , "PostOrder" , "InOrder" , "LevelOrder"]

                   },
                   {
                      q: "9 . What is the time complexity of binary tree insertion ? ",
                      opts: ["O(1)" , "O(log n)" , "O(n)" , "O(n logn)"]

                   },

                   {
                      q: "10 . If inorder and preorder traversals are given, can we construct the tree ? ",
                      opts: ["Yes, uniquely" , "No" , "Sometimes" , "Only if tree is BST"]

                   },
                   {
                     q: `11 . In AVL tree, balance factor is ? `,
                    opts: ["Height(left) + Height(right)" , 
                    "Height(left) − Height(right)" , "Level difference" ,
                     "Total nodes difference"]
                   },
                   {
                     q: "12 . If a full binary tree has L leaf nodes, total number of nodes is ? ",
                     opts: ["2L" , "2L-1" , "L-1" , "L²"]
                   },
                   {
                     q: "13 . Number of binary trees possible with 3 nodes ? ",
                     opts: ["3" , "5" , "6" ,"8"]
                   },
                   {
                    q: "14 . Which data structure is most appropriate to represent this system ? ",
                    opts: ["Queue" , "Stack" , "Tree" ,"Graph"]
                   },
                   {
                     q: "15 . Real Life Application of Tree Data Structure ? ",
                      opts: ["Shortest Path" , "Searching in DataBase" ,"Dictionary Apps" , "All of above"]
                   }
             ]; %>

             <% const arr = [] %>

             <% questions.forEach((item , i) =>{ %>

              <div class="question">
                <h3><%=item.q %></h3>
                <% item.opts.forEach((opt , j) => { %>
                       <label>
                         <input type ="radio" name ="q<%= i + 1 %>" value ="<%= opt %>" class ="q<%= i+1 %>" required>
                         <%= opt %>
                       </label>
               <% }) %>
              </div>
             <% })%>

             <button type = "submit" class="btn5-quiz" >Submit Quiz</button>

          </form>

       </div>
     </div>
 <script src ="/script/tree.js"></script>


</body>
</html>