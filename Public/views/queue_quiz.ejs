<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Queue Quiz</title>
    <link rel="stylesheet" href="/style/nav.css">
<style>
     * {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  }

body {
  font-family: "Poppins", sans-serif;
  margin: 0;
  padding: 0;
  background: linear-gradient(135deg, #74ABE2, #5563DE);
  color: #fff;
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 100vh;
}

/* Adjust content below navbar */
.content-wrapper {
  margin-top: 90px; /* navbar height */
  width: 100%;
  display: flex;
  justify-content: center;
  padding: 0 15px; /* prevents overflow on small screens */
  box-sizing: border-box;
}

.quiz_container {
  background: #ffffff15;
  backdrop-filter: blur(15px);
  border-radius: 16px;
  width: 100%;
  max-width: 800px; /* keeps container inside screen */
  padding: 30px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.3);
  transition: all 0.3s ease;
  margin-bottom: 60px;
}

h1 {
  text-align: center;
  margin-bottom: 25px;
  color: #fff;
  font-size: 2rem;
}

.question {
  background: rgba(255,255,255,0.1);
  padding: 15px;
  border-radius: 10px;
  margin-bottom: 20px;
  transition: all 0.3s ease;
}

.question:hover {
  background: rgba(255,255,255,0.2);
}

.question h3 {
  font-size: 1.1rem;
  margin-bottom: 10px;
  color: #f3f3f3;
}

label {
  display: block;
  margin-bottom: 8px;
  cursor: pointer;
}

input[type="radio"] {
  accent-color: #00ffbf;
  margin-right: 8px;
}

button {
  background: #00ffc8;
  border: none;
  color: #333;
  padding: 12px 25px;
  font-size: 1rem;
  border-radius: 10px;
  cursor: pointer;
  margin: 20px auto 0;
  display: block;
  transition: all 0.3s ease;
}

button:hover {
  background: #00bfa5;
  color: #fff;
  transform: scale(1.05);
}

/* ===== Responsive ===== */
@media screen and (max-width: 900px) {
  .quiz_container {
    padding: 20px;
  }

  h1 {
    font-size: 1.6rem;
  }

  .question h3 {
    font-size: 1rem;
  }
}

@media screen and (max-width: 600px) {
  /* Ensure navbar links stay inside screen */
  .content-wrapper {
    padding: 0 10px;
  }

  .quiz_container {
    padding: 15px;
  }

  h1 {
    font-size: 1.4rem;
  }

  .question h3 {
    font-size: 0.95rem;
  }

  label {
    font-size: 0.9rem;
  }
}
</style>


</head>
<body>

    <%- include('partials/nav') %>
    
     <div class ="content-wrapper">
       <div class="quiz_container">
         <h1>Queue Quiz</h1>

          <form  action="/submit-queue-quiz" method="POST" id="queueForm">

             <% const questions = [

                   {
                      q: "1 . What is the principle followed by Queue data structure ? ",
                      opts: ["LIFO" , "FIFO" , "LILO" , "FILO"]
                   },
                   {
                      q: "2 . Which operation inserts an element into a queue? ",
                      opts: ["push()" , "pop()" , "enqueue()" , "insert()"]
                   } ,
                   {
                      q: "3 . Which operation removes an element from a queue? ",
                      opts: ["delete()" , "dequeue()" , "pop()" , "removeLast()"]

                   },
                   {
                     q: "4 . In a queue, deletion happens from ? ",
                     opts: [ "Rear" , "Front" , "Middle" , "Any position"]

                   },
                   {
                      q: "5 . In a queue, insertion happens at ? ",
                      opts: ["Front" , "Middle" ,"Rear" , "Random position"]

                   },
                   {
                      q: "6 . Which data structure is mainly used to implement BFS internally ? ",
                      opts: ["Queue" , "Heap" , "Stack" , "Linked List"]

                   },
                   {
                      q: "7 . Which real-life example represents a queue? ",
                      opts: ["Undo / Redo " , "Function calls" , "Expression Evaluation" , "People standing in the line"]

                   },
                   {
                      q: "8 . What is queue underflow ? ",
                      opts: ["Queue is Full" , "Rear becomes 0" , "Queue has one element" , "Queue is empty and dequeue is performed"]

                   },
                   {
                      q: "9 . What is the time complexity of enqueue() in array implementation ? ",
                      opts: ["O(1)" , "O(log n)" , "O(n)" , "O(n logn)"]

                   },

                   {
                      q: "10 . In a simple array queue of size 5, what condition indicates overflow ? ",
                      opts: ["front == rear" , "rear == size - 1" , "rear == 0 " , "front == -1"]

                   },
                   {
                     q: `11 . In priority queue, elements are removed based on ? `,
                    opts: ["Insertion order" , "FIFO" , "Priority" , "Random order"]
                   },
                   {
                     q: "12 . What is the space complexity of queue storing n elements ? ",
                     opts: ["O(1)" , "O(log n)" , "O(n²)" , "O(n)"]
                   },
                   {
                     q: "13 . What is the advantage of circular queue over simple queue? ",
                     opts: ["Faster execution" , "Less memory usage" , "Avoids wasted space" ,"Easier implementation"]
                   },
                   {
                    q: "14 . Which problem can be solved using queue ? ",
                    opts: ["Balanced Parentheses" , "Level Order Traversal" , "Infix to Postfix" ,"Recursion"]
                   },
                   {
                     q: "15 . In a circular queue, overflow occurs when ? ",
                      opts: ["rear == size - 1" , "rear == front" ,"(rear + 1) % size == front" , "front == -1"]
                   }
             ]; %>

             <% const arr = [] %>

             <% questions.forEach((item , i) =>{ %>

              <div class="question">
                <h3><%=item.q %></h3>
                <% item.opts.forEach((opt , j) => { %>
                       <label>
                         <input type ="radio" name ="q<%= i + 1 %>" value ="<%= opt %>" class ="q<%= i+1 %>" required>
                         <%= opt %>
                       </label>
               <% }) %>
              </div>
             <% })%>

             <button type = "submit" class="btn4-quiz" >Submit Quiz</button>

          </form>

       </div>
     </div>
 <script src ="/script/queue.js"></script>


</body>
</html>